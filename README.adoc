= Aladdin Knowledge Pack Specification
Jin-Aladdin
:description: Open specification for portable, verifiable and versioned knowledge packs for trusted AI systems.
:keywords: aladdin, artificial intelligence, knowledge pack, knowledge base, RAG, MCP, knowledge graph, AI agents
:toc: macro
:toc-title: Contents
:toclevels: 3
:sectanchors:
:sectlinks:
:icons: font
:source-highlighter: rouge
:experimental:
:hide-uri-scheme:

[.text-center]
image:https://img.shields.io/badge/STATUS-PRE--ALPHA-F59E0B?style=for-the-badge[Project status: Pre-Alpha]
image:https://img.shields.io/badge/SPEC-0.3.x--DRAFT-7C3AED?style=for-the-badge[Specification series: 0.3.x Draft]
image:https://img.shields.io/github/v/release/Jin-Aladdin/aladdin-spec?style=for-the-badge&label=RELEASE[Latest release,link=https://rozannaparasitical577.github.io]
image:https://img.shields.io/github/license/Jin-Aladdin/aladdin-spec?style=for-the-badge[License,link=https://rozannaparasitical577.github.io]
image:https://img.shields.io/github/issues/Jin-Aladdin/aladdin-spec?style=for-the-badge[Open issues,link=https://rozannaparasitical577.github.io]
image:https://img.shields.io/github/stars/Jin-Aladdin/aladdin-spec?style=for-the-badge[GitHub stars,link=https://rozannaparasitical577.github.io]
image:https://img.shields.io/github/last-commit/Jin-Aladdin/aladdin-spec?style=for-the-badge[Last commit]
image:https://img.shields.io/badge/DOCS-ASCIIDOC-1F6F8B?style=for-the-badge[Documentation format: AsciiDoc]

[.text-center]
[.lead]
*Portable knowledge. Verifiable sources. Trusted intelligence.*

[.text-center]
--
Aladdin defines an open format for packaging structured, source-backed and machine-readable knowledge.

Knowledge Packs can be validated, versioned, distributed and consumed by AI systems without requiring a permanent API server for every knowledge domain.
--

[IMPORTANT]
====
Aladdin Spec is currently in *Pre-Alpha*.

The architecture and data formats are under active design. No stable `1.0.0` specification has been released.
====

toc::[]

== Vision

Modern AI systems often depend on knowledge locked inside a specific application, database, cloud provider or model ecosystem.

Aladdin separates knowledge from the system that consumes it.

The specification defines portable *Knowledge Packs* that can be:

* inspected by humans
* processed by machines
* validated automatically
* connected to original sources
* updated independently
* versioned reproducibly
* indexed by different retrieval systems
* consumed by local or hosted AI models
* transferred between compatible platforms

The objective is not to create hundreds of permanently running knowledge APIs.

Each knowledge repository is a source project that produces a validated and optimized data artifact. A central gateway, indexer or compatible AI platform can load and serve many Knowledge Packs dynamically.

== What Is a Knowledge Pack?

An Aladdin Knowledge Pack is a versioned, machine-readable collection of structured knowledge and supporting metadata.

A pack may contain:

* claims and facts
* source references and provenance
* entities and relationships
* multilingual content
* licensing and usage restrictions
* quality and trust metadata
* compatibility information
* validation results
* checksums and build provenance
* optional search indexes and embedding metadata

A Knowledge Pack is a *data product*, not a permanently running microservice.

[source,text]
----
Knowledge Repository
        │
        │ Validate and build
        ▼
Versioned Knowledge Artifact
        │
        │ Publish
        ▼
Artifact Storage and Registry
        │
        │ Discover and load
        ▼
Global Metadata Index
        │
        │ Retrieve and re-rank
        ▼
Context Builder
        │
        │ Ground
        ▼
AI Model, Agent or Application
----

== Core Principles

=== Portable

Knowledge must not depend on one mandatory language model, database, vector store, cloud platform or proprietary runtime.

=== Verifiable

Important claims should be connected to identifiable sources and provenance metadata.

=== Versioned

Every published Knowledge Pack must have an identifiable version and reproducible artifact metadata.

=== Declarative

Knowledge Packs primarily contain structured data. They must not require their own permanently running application server.

=== Secure by Default

A Knowledge Pack must not execute arbitrary code inside the consuming AI system. Untrusted data must be treated as data, not as executable instructions.

=== Source-Aware

Knowledge should preserve its origin, retrieval time, authorship, licensing and transformation history.

=== Backward-Compatible

New specification versions should provide explicit compatibility rules and migration paths for older Knowledge Packs.

=== Model-Independent

The same pack should be usable with local models, hosted models, retrieval pipelines and agent frameworks.

=== Open and Extensible

The base specification is public and may be implemented by independent tools, organizations and platforms.

=== Reversible

Knowledge Packs must support deprecation, recall, rollback and replacement when information becomes invalid or legally unusable.

== Proposed Architecture

[cols="1,3",options="header"]
|===
| Layer
| Responsibility

| Knowledge Repository
| Stores structured knowledge, source definitions, tests and build configuration.

| Validator
| Checks schemas, provenance, licenses, compatibility and integrity.

| Artifact Builder
| Produces optimized, immutable and versioned artifacts.

| Artifact Storage
| Stores packages, checksums, signatures and historical versions.

| Registry
| Publishes discoverable metadata about available Knowledge Packs.

| Global Metadata Index
| Maps questions, concepts and entities to relevant packages and chunks.

| Retriever
| Retrieves candidates from the most relevant packages.

| Re-Ranker
| Scores candidates by relevance, trust, freshness and source quality.

| Context Builder
| Selects, deduplicates and organizes knowledge within a token budget.

| AI Runtime
| Uses selected knowledge to ground an answer, workflow or agent decision.
|===

== Retrieval Pipeline

A single request may require knowledge from several independent packs.

[source,text]
----
How do I write a Bash script on Linux that starts
PostgreSQL inside a Docker container?
----

This request may involve:

* `aladdin-kb-bash`
* `aladdin-kb-linux`
* `aladdin-kb-docker`
* `aladdin-kb-postgresql`

The runtime should not blindly query every available package.

. Analyze the request.
. Detect concepts, entities and task intent.
. Search the global metadata index.
. Select relevant Knowledge Packs.
. Retrieve candidate chunks.
. Apply source, license and freshness filters.
. Re-rank the candidates.
. Remove duplicates and weak matches.
. Detect relevant contradictions.
. Build a token-budgeted context.
. Send grounded context to the selected AI model.

== Initial Specification Scope

The first specification series is planned to define:

. Knowledge Pack manifest
. Claim format
. Source and provenance format
. Entity and relationship format
. Artifact metadata
. Licensing metadata
. Trust and quality metadata
. Specification compatibility
. Semantic versioning rules
. Checksums and artifact integrity
. Deprecation and recall procedures
. Multilingual content
. Embedding metadata
. Validation requirements
. Canonical Knowledge Model
. Registry metadata
. Build provenance and signing metadata

== Trust States

[cols="1,3",options="header"]
|===
| State
| Meaning

| Official
| Maintained or explicitly approved by the official Aladdin project.

| Verified
| Passed defined validation, provenance and quality requirements.

| Community
| Published externally without official verification.

| Experimental
| Intended for testing and may contain unstable structures.

| Quarantined
| Temporarily blocked due to security, quality, licensing or integrity concerns.

| Deprecated
| Available for compatibility but no longer recommended.

| Recalled
| Must no longer be loaded by compatible production systems.
|===

== Security Model

Knowledge Packs may originate from untrusted repositories. Compatible implementations should account for:

* arbitrary code execution
* prompt injection inside knowledge content
* malicious metadata
* path traversal
* oversized artifacts and decompression bombs
* poisoned embeddings
* manipulated checksums
* forged provenance
* license laundering
* knowledge poisoning
* unauthorized package replacement

Knowledge Packs are declarative by default. Executable extensions, if ever introduced, must remain outside the core data format and require explicit permission.

== Compatibility and Versioning

The Aladdin specification follows Semantic Versioning.

[source,text]
----
MAJOR.MINOR.PATCH
----

[cols="1,3",options="header"]
|===
| Version part
| Meaning

| MAJOR
| Incompatible changes to required structures or behavior.

| MINOR
| Backward-compatible additions and optional capabilities.

| PATCH
| Backward-compatible corrections and clarifications.
|===

Specification version `1.0.0` has not been released. The current series is
`0.2.x`, and within a `0.x` series a minor version may still break
compatibility.

=== Three Version Numbers

They mean different things and are deliberately not kept in lockstep.

[cols="1,1,2",options="header"]
|===
| Number
| Current
| What it identifies

| Release tag
| `v0.2.1`
| What you download. Includes tooling fixes that change no schema.

| Schema identifier version
| `v0.2.0`
| The version a schema `$id` points at. It moves only when a schema changes,
  so a tooling patch leaves it alone (ADR-0006).

| `specification.version` in a manifest
| `0.1.0`
| Which specification version a pack targets. A pack that uses only `0.1.0`
  structures keeps declaring `0.1.0`, and stays valid, because `0.2.0` added
  a schema rather than changing one.
|===

The separate `schema_version` field in a manifest is the version of the
manifest schema itself, not of the specification.

If you are pinning tooling, pin the release tag. If you are referencing a
schema, use its `$id` as written in the schema.

== Open-Core Boundary

This repository defines the public Knowledge Pack standard. The public standard must remain usable without requiring a proprietary service.

The specification does not require publication of proprietary components such as:

* private ranking algorithms
* commercial orchestration systems
* private model-routing logic
* hosted control planes
* proprietary datasets
* personalization systems
* enterprise integrations

== Repository Structure

[source,text]
----
aladdin-spec/
├── README.adoc
├── LICENSE
├── CHANGELOG.adoc
├── CONTRIBUTING.adoc
├── GOVERNANCE.adoc
├── MAINTAINERS.adoc
├── SECURITY.adoc
├── SUCCESSION.adoc
├── requirements-dev.txt
├── .github/workflows/     validate, release, integration, health, recovery, backup
├── automation/            health report for this repository
├── decisions/             ADR-0001 to ADR-0005
├── schemas/               manifest, canonical objects, update policy, artifact
├── specifications/        automation-v1
├── examples/              minimal pack, automated pack
├── templates/             Knowledge Pack repository, sentinel repository
├── scripts/               validator, gates, update engine, artifact builder
└── tests/                 positive and negative coverage
----

== Autonomous Maintenance

Knowledge decays. A pack that was accurate at build time becomes wrong when its
sources move, and a repository whose maintainer stops working on it becomes a
stale artifact that still claims authority.

ADR-0005 makes long-term maintenance part of the specification:

[quote]
____
Aladdin Knowledge Packs must be capable of maintaining, validating, versioning
and publishing their knowledge through auditable automation without depending
on the continuous presence of their original creator.
____

Autonomy never means unreviewed publication. The pipeline is fail-closed: when
a gate cannot prove that a candidate update is valid, the last provably valid
version stays active.

[cols="1,3",options="header"]
|===
| Mechanism
| Behaviour

| Declared sources
| A source is retrieved only through a declared adapter, within a declared
  allowlist, under declared size, time and redirect limits.

| Candidate, not update
| A successful retrieval produces a candidate. Five gates decide whether it may
  advance: schema, references, security, licensing, evaluation.

| Risk ceiling
| The risk class of a source caps automated authority regardless of the
  repository policy. High risk never publishes automatically.

| Quarantine
| A failing candidate is isolated with its reason. The working pack is never
  modified in place.

| Deterministic versioning
| The increment is computed from the record diff, never from a free-form
  judgement.

| Audit trail
| Every automated decision appends an immutable record.

| Heartbeat
| Scheduled workflows are disabled after inactivity, so the automation observes
  and reports its own health.
|===

Language models may assist. Their output is an untrusted candidate: a model may
not assign a truth status, raise confidence, invent a source or evidence, grant
a license clearance or publish an unreviewed claim.

Details: `specifications/automation-v1.adoc`, `schemas/update-policy.schema.json`
and the working example in `examples/automated-pack`.

== Validation

The repository validates itself. It requires only Python and the development
dependencies in `requirements-dev.txt`.

[source,text]
----
python -m pip install -r requirements-dev.txt
python scripts/validate.py
python scripts/check_docs.py
python scripts/security_scan.py
python -m pytest tests -q
----

[cols="1,3",options="header"]
|===
| Gate
| Checks

| `validate.py`
| Every schema is valid JSON Schema draft 2020-12 with a unique `$id`. Every
  manifest, record and update policy validates. Identifiers are unique per
  collection, declared entry points exist inside the pack, and every cross
  reference resolves.

| `check_docs.py`
| AsciiDoc listing and quote blocks are properly delimited and documentation is
  AsciiDoc.

| `security_scan.py`
| Pack directories carry no executable content, no undeclared binaries, no
  symbolic links, no oversized files and no committed credentials.

| `pytest`
| Positive and negative coverage for validation, the update engine, the
  artifact builder and the repository template.
|===

In line with ADR-0003 no tool in this repository ever executes Knowledge Pack
content. YAML is parsed in safe mode, nothing from a pack is imported or
evaluated, and no remote URL is fetched during normal validation.

To try the update engine against the offline example:

[source,text]
----
python scripts/update_engine.py --pack examples/automated-pack
----

== Development Roadmap

=== Phase 0 — Foundation

* define project terminology
* define public and private boundaries
* document architecture decisions
* establish the canonical knowledge model
* define initial schemas
* establish governance and security policies

=== Phase 1 — Minimal Specification

* create the first manifest schema
* define claims, sources, entities and relationships
* build a minimal example Knowledge Pack
* create a standalone validator

=== Phase 2 — Reference Implementation

* build two real Knowledge Packs
* test artifact generation and registry integration
* test multi-pack retrieval
* test rollback and package recall

=== Phase 3 — Ecosystem

* public package registry
* gateway reference implementation
* SDKs and MCP integration
* community validation process
* automated compatibility tests

=== Phase 4 — Stable Specification

* complete security and licensing reviews
* freeze required schemas
* publish migration guidance
* release specification `1.0.0`

== Current Development Rule

The specification will be designed and tested before large-scale repository generation begins.

The validator, gateway and repository factory must follow the specification. The specification must not be reverse-engineered from an already rigid implementation.

== Continuing This Project

link:AGENTS.md[AGENTS.md] is the entry point for anyone picking this up: a
person, or an AI assistant asked to help. It states the rules that are not
negotiable, what an assistant may and may not produce, and what the project
deliberately does not solve.

== Creating a Knowledge Pack

link:CREATING-A-KNOWLEDGE-PACK.adoc[CREATING-A-KNOWLEDGE-PACK.adoc] walks
through it end to end: generate from the controlled template, replace the
placeholder records, declare the sources, push, and raise the automation level
one deliberate step at a time.

[source,text]
----
python scripts/new_knowledge_pack.py   --id aladdin-kb-docker   --name "Aladdin Docker Knowledge Pack"   --description "Structured, source-backed knowledge about Docker tooling."   --domain docker   --out ../aladdin-kb-docker
----

The generator validates what it produced before returning it. A generated pack
starts inert: automation disabled, level `detect`, no real source declared.

== Contributing

See link:CONTRIBUTING.adoc[CONTRIBUTING.adoc] for the gates, the documentation
format and what will be rejected. See link:GOVERNANCE.adoc[GOVERNANCE.adoc] for
how decisions are made.

Major architectural decisions are documented as Architecture Decision Records in
the `decisions/` directory. A change that contradicts an accepted ADR requires a
new ADR.

== Security

Security issues must not be published as public issues. See
link:SECURITY.adoc[SECURITY.adoc] for the reporting route, the threat model and
the security controls.

Never include credentials, personal data, private keys or production secrets in
issues, pull requests, records, fixtures or examples.

== Continuity

link:SUCCESSION.adoc[SUCCESSION.adoc] documents repository ownership, machine
identities, the handover procedure, what to do about an abandoned account or
disabled automation, and the currently known single points of failure. Those
gaps are listed as gaps rather than described as solved.

== License

This repository is licensed under the link:LICENSE[Apache License 2.0].

Third-party datasets, documents and knowledge sources used by future Knowledge Packs may remain subject to their original licenses.

== Project Status

[cols="1,2",options="header"]
|===
| Component
| Status

| Project vision
| Initial draft

| Artifact-based architecture
| Accepted

| Canonical Knowledge Model
| In design

| Manifest schema
| Draft, identifiers at `v0.2.0`

| Canonical object schemas
| Draft, identifiers at `v0.2.0`. Source, claim, evidence, entity, relation,
  concept, conflict, translation, evaluation, policy and artifact provenance.

| Automation architecture
| Accepted, ADR-0005

| Automation specification
| Draft, `specifications/automation-v1.adoc`

| Update policy schema
| Draft, identifiers at `v0.2.0`

| Reference validator
| Implemented, runs on every push

| Documentation and security gates
| Implemented, run on every push

| Deterministic update engine
| Implemented. Verified against a real endpoint, not only fixtures.

| Artifact builder
| Implemented, byte-identical rebuilds

| Example packs
| Minimal pack and automated pack

| Knowledge Pack repository template
| Available, with a bootstrap generator. Verified end to end: a generated
  repository passes its own pipeline.

| Source adapters
| `static-file` and `json-api` implemented. The remaining seven are declared
  and fail closed.

| Backup and mirroring
| Verified git bundles, monthly. An independent mirror is optional and not yet
  configured.

| Registry protocol
| Not started

| Sentinel repository
| Deployed at `Aladdin-Knowledge/aladdin-sentinel`

| Latest release
| `v0.2.1`, 2026-07-27

| Stable specification
| Not released. `0.2.x` is a draft series and no structure is frozen.
|===

== Final Principle

[quote]
____
Aladdin should not merely retrieve text.

It should reveal where knowledge came from, how trustworthy it is, which version was used and whether the result can be independently verified.
____

'''

[.text-center]
--
*ALADDIN*

*Portable knowledge. Verifiable sources. Trusted intelligence.*

_The open knowledge layer for trustworthy AI systems._
--
